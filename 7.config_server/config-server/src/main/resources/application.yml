server:
  port: 8081

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/DarkFire414/microservicios-spring-cloud.git
          searchPaths: 7.config-server/config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}